# Demonstration data

Both CSV files are deterministic and completely synthetic. `synthetic_qualtrics_evaluation.csv` contains 128 response rows and a few fake PII patterns solely to demonstrate masking. `synthetic_course_history.csv` illustrates a future comparison input; the MVP does not claim trend comparability automatically.

